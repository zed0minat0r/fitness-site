# Agent Rules

## CRITICAL — START WITH A TOOL CALL
- Your VERY FIRST action must be a tool call. Do NOT generate ANY text before your first tool call.

## Shared Rules
- Site capped at 10-12 sections. No free additions.
- MOBILE FIRST.
- No desktop-only features (hover effects, cursor interactions).
- Only read the LAST 10 lines of CHANGELOG.md.
- Use targeted grep/read instead of reading entire files.
- ALWAYS text user via iMessage after completing work.
- Keep animations MINIMAL and tasteful.
